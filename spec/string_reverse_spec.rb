require 'spec_helper'

describe StringReverse do
  it 'has a version number' do
    expect(StringReverse::VERSION).not_to be nil
  end

  it 'does something useful' do
    expect(false).to eq(true)
  end
end
